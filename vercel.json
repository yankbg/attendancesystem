{
  "functions": {
    "api/**/*.php": {
      "runtime": "vercel-php@0.6.0"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },  // Route API calls to PHP functions
    { "src": "/(.*)", "dest": "/index.html" }  // Serve index.html for all other routes (SPA support)
  ]
}
